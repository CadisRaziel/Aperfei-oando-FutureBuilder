import 'package:alura_sqlite/src/core/app_widget.dart';
// import 'package:alura_sqlite/src/database/app_database.dart';
// import 'package:alura_sqlite/src/models/contact.dart';
// import 'package:flutter/foundation.dart';
import 'package:flutter/material.dart';

void main() {
  runApp(const MyApp());

  //para salvar contato, conforme fui criando era importante fazer isso
  // save(Contact(id: 0, name: 'Batman', number: 500)).then((id) {
  //   findAll().then((contacts) => debugPrint(contacts.toString()));
  // });
}
